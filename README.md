# mBoot
Hypervisor for [mochiOS](https://github.com/tas0dev/mochiOS)

## build
After running `git submodule update --init --recursive` and `make setup`,
place the generated files `mochiOS.img` and `esp.img` from building mochiOS in the root of this repository, and then run `make`.
